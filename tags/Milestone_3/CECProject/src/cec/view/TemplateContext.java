package cec.view;

/** Auxiliary enum used by <code>EmailFrame</code> class
 *  to support the process of handling an Email Templates in 
 *  each different context (NEW, EDIT, APPLY).
*/

public enum TemplateContext {
	NEW, EDIT, APPLY
}
